version: "3"

networks:
  catamongo.net:
    driver: bridge

services:
  php:
    image: canals/php:8.0
    ports:
      - "12080:80"
      - "12443:443"
    volumes:
      - .:/var/www
    working_dir: /var/www/public
    networks:
      - catamongo.net

  ################################################
  # mongodb & mongo-express
  ###############################################
  mongo:
    image: mongo:latest
    volumes:
    - ./data:/var/data
    working_dir: /var/data
    networks:
    - catamongo.net
    ports:
    - 27017:27017